# 当前状态说明

## 目前已经能做什么

当前项目后端已经可以完成一条最小链路：

- 启动 API 服务
- 加载杭州政府示例数据源
- 抓取部分公告详情页
- 入库到本地数据库
- 通过搜索接口查结果
- 通过问答接口返回带引用的回答

## 为什么你感觉“搜不出来”

主要有两个原因：

### 1. 现在库里的数据还很少

目前只采集并入库了少量杭州通知公告示例数据。  
所以并不是“任何关键词都能搜出来”，而是只有和当前入库内容相关的关键词才有结果。

例如当前可以命中的词包括：

- 工匠
- 技能
- 人才

如果你搜一个和当前两条数据完全无关的词，自然会返回空结果。

### 2. PowerShell 里中文显示可能乱码

我在终端里测试接口时，返回内容有时会显示乱码。  
这是 PowerShell 控制台编码显示问题，不代表接口数据本身坏了。

在浏览器打开接口文档时，正常情况下应按 UTF-8 显示中文。

## 我已经实际验证过的接口

### 健康检查

`GET /health`

返回正常。

### 数据源

`GET /sources`

可以返回杭州政府示例数据源。

### 采集接口

`POST /ingest/run`

我实际调用后返回过类似结果：

```json
{
  "sources": {
    "hangzhou_gov": {
      "fetched": 2,
      "stored": 0
    }
  }
}
```

其中：

- `fetched: 2` 表示本次抓到了 2 条
- `stored: 0` 表示这 2 条之前已经入库，所以这次没有新增

### 搜索接口

`GET /documents/search?q=工匠`

可以查到结果。

`GET /documents/search?q=技能`

也可以查到结果。

### 问答接口

`POST /ask`

用下面这个问题可以返回结果：

```json
{
  "question": "杭州工匠相关通知有哪些？",
  "top_k": 3
}
```

## 你现在怎么自己测试

先启动服务，然后打开：

`http://127.0.0.1:8081/docs`

建议按这个顺序测：

1. 先测 `GET /health`
2. 再测 `GET /sources`
3. 再测 `POST /ingest/run`
4. 再测 `GET /documents/search`
5. 最后测 `POST /ask`

## 推荐测试参数

### 采集

```json
{
  "source_keys": ["hangzhou_gov"],
  "limit_per_channel": 2
}
```

### 搜索词

建议先试这些：

- 工匠
- 技能
- 人才

### 问答

```json
{
  "question": "杭州工匠相关通知有哪些？",
  "top_k": 3
}
```

## 当前的真实问题

现在的问题不是“系统完全不能搜”，而是：

- 数据量太少
- 只有后端，没有前台页面
- 站点覆盖范围还很小
- 问答还只是检索汇总，不是真正大模型生成版

## 下一步最值得做的事

如果继续往前推，优先级建议是：

1. 扩大采集量，让库里先有更多数据
2. 做一个最小前台页面，方便直接搜索和提问
3. 接 PostgreSQL 和向量检索
4. 扩展更多政务站点配置和适配器
5. 最后再接真实大模型

## 结论

当前项目不是“完全跑不通”，而是：

- 主链已经通了
- 能查到少量已入库数据
- 只是还远没到可用产品阶段

如果后面继续开发，优先要解决的是“数据规模”和“前台交互”，而不是先纠结模型。
