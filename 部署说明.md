# Gov RAG 服务器部署

## 1. 服务器要求
- Ubuntu 22.04/24.04
- 已安装 Docker 和 Docker Compose
- 开放 `80` 端口

## 2. 上传项目
把整个 `gov-rag` 目录上传到服务器，例如：

```bash
/opt/gov-rag
```

## 3. 准备环境变量
确认根目录下 `.env` 存在，并至少包含：

```env
APP_NAME=Gov RAG
APP_ENV=production
HOST=0.0.0.0
PORT=8081
DATABASE_URL=sqlite:///./gov_rag.db
LLM_PROVIDER=ark
LLM_MODEL=doubao-seed-2-0-mini-260215
OPENAI_API_KEY=你的火山 key
OPENAI_BASE_URL=https://ark.cn-beijing.volces.com/api/v3
LLM_TIMEOUT_SECONDS=120
```

注意：
- 容器实际会把数据库改到 `/data/gov_rag.db`
- `app/sources` 会直接挂载，方便你后续继续加 yaml

## 4. 启动

```bash
cd /opt/gov-rag
docker compose -f docker-compose.server.yml up -d --build
```

## 5. 查看状态

```bash
docker compose -f docker-compose.server.yml ps
docker compose -f docker-compose.server.yml logs -f backend
docker compose -f docker-compose.server.yml logs -f frontend
```

## 6. 访问
- 前端：`http://你的服务器IP/`
- 后端健康检查：`http://你的服务器IP/api/health`

## 7. 重启

```bash
cd /opt/gov-rag
docker compose -f docker-compose.server.yml restart
```

## 8. 更新代码后重新发布

```bash
cd /opt/gov-rag
docker compose -f docker-compose.server.yml up -d --build
```
